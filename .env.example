# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# [NEED: country-specific environment variables]
# Transport endpoints, credentials, and environment switches are added once the
# specification under specs/ documents them. See the package's own compliance reference.
